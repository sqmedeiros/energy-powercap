compile: $(patsubst %.java, %.class, $(wildcard *.java))

%.class: %.java Makefile
	javac $<
	python3 ../../run-experiment-script/perf-rapl.py "java $* < "  "$(OUTPUT)" ../$(PROBLEM) $@	"$(TEST)"
		
clean:
	rm *.class

