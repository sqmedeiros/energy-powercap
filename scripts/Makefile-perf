compile: $(patsubst %.cpp, %.exe, $(wildcard *.cpp))

%.exe: %.cpp Makefile
	g++ $< -o $@ $(CPPFLAGS)
	sudo prlimit --stack=unlimited python3 ../../run-experiment-script/perf-rapl.py "./$@ < "  "$(OUTPUT)" ../$(PROBLEM) $@	"$(TEST)"
		
clean:
	rm *.exe

