//package mathematics.prime_multiples;

import java.io.BufferedInputStream;
import java.io.PrintWriter;

class FastReader {
    private int BS = 1 << 16;
    private char NC = (char) 0;
    private byte[] buf = new byte[BS];
    private int bId = 0, size = 0;
    private char c = NC;
    private double cnt = 1;
    private BufferedInputStream in;

    public FastReader() {
        in = new BufferedInputStream(System.in, BS);
    }


    private char getChar() {
        while (bId == size) {
            try {
                size = in.read(buf);
            } catch (Exception e) {
                return NC;
            }
            if (size == -1) return NC;
            bId = 0;
        }
        return (char) buf[bId++];
    }

    public int readInt() {
        return (int) readLong();
    }

    public int[] readInts(int N) {
        int[] res = new int[N];
        for (int i = 0; i < N; i++) {
            res[i] = (int) readLong();
        }
        return res;
    }

    public long[] readLongs(int N) {
        long[] res = new long[N];
        for (int i = 0; i < N; i++) {
            res[i] = readLong();
        }
        return res;
    }

    public long readLong() {
        cnt = 1;
        boolean neg = false;
        if (c == NC) c = getChar();
        for (; (c < '0' || c > '9'); c = getChar()) {
            if (c == '-') neg = true;
        }
        long res = 0;
        for (; c >= '0' && c <= '9'; c = getChar()) {
            res = (res << 3) + (res << 1) + c - '0';
            cnt *= 10;
        }
        return neg ? -res : res;
    }

    public double nextDouble() {
        double cur = readLong();
        return c != '.' ? cur : cur + readLong() / cnt;
    }

    public double[] nextDoubles(int N) {
        double[] res = new double[N];
        for (int i = 0; i < N; i++) {
            res[i] = nextDouble();
        }
        return res;
    }

    public String next() {
        StringBuilder res = new StringBuilder();
        while (c <= 32) c = getChar();
        while (c > 32) {
            res.append(c);
            c = getChar();
        }
        return res.toString();
    }

    public String nextLine() {
        StringBuilder res = new StringBuilder();
        while (c <= 32) c = getChar();
        while (c != '\n') {
            res.append(c);
            c = getChar();
        }
        return res.toString();
    }

    public boolean hasNext() {
        if (c > 32) return true;
        while (true) {
            c = getChar();
            if (c == NC) return false;
            else if (c > 32) return true;
        }
    }
}

public class entry_4741139 {
    private PrintWriter writer;
    private FastReader reader;
    long[] primes;
    long n;
    long count;
    private void iteratePrimeSubsets(long accumulatedProduct, int index, int sign){
        if (index == primes.length){
            if (accumulatedProduct != 1) count += sign*(n/accumulatedProduct);
            return;
        }
        iteratePrimeSubsets(accumulatedProduct, index+1, sign);
        if (accumulatedProduct <= n/primes[index]){
            iteratePrimeSubsets(accumulatedProduct*primes[index], index+1,  -sign);
        }

    }
    private void computeMultiples(){
        writer = new PrintWriter(System.out);
        reader = new FastReader();
        n = reader.readLong();;
        int k = reader.readInt();
        primes = reader.readLongs(k);
        iteratePrimeSubsets(1, 0, -1);
        writer.println(count);
        writer.close();

    }

    public static void main(String[] args) {
        new entry_4741139().computeMultiples();
    }
}